"""import math
class calculate:
    def rectangle(self, a, b):
        self.a = a
        self.b = b
        print("perimeter of a rectangle", 2*(a + b))
        print("area of a rectangle", (a * b))

    def circle(self, a):
        self.a = a
        print("Area of circle is ", 2 * math.pi * a * a)
        print("perimeter of circle is ", math.pi * a * a)

    def triangle(self, a, b, c):
        self.a = a
        self.b = b
        self.c = c
        s = a + b + c / 2

        #print("area of tringle is ", math.sqrt(s(s - a) * (s - b) * (s - c)))
        print("perimeter of triangle is", (a + b + c))
obj = calculate()
obj.rectangle(20, 40)
obj.circle(5)
obj.triangle(3, 4, 5)
"""
"""n = int(input("enter value of n:"))
r = n*(n + 1) / 2
print(r)"""
